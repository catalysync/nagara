# Config reference

Generated by `scripts/generate-config-docs.py` — do not edit by hand.

Every key here is readable via the `NAGARA_*` env vars, from a 
`[tool.nagara]` table in `pyproject.toml`, from `~/.config/nagara/
config.toml`, or from a named profile in `~/.config/nagara/profiles.toml`.
See [development.md](./development.md) for the full priority order.

| Name | Type | Default | Constraints | Description |
|------|------|---------|-------------|-------------|
| `ENV` | `<enum 'Environment'>` | `'development'` | — | Deployment environment. Drives .env file selection and prod-only guardrails. |
| `APP_NAME` | `str` | `'nagara'` | — | Display name in API docs, logs, metrics. |
| `APP_VERSION` | `str` | `'0.1.0'` | — | Version string surfaced in /openapi.json. |
| `LOG_LEVEL` | `Literal['DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL'] \| None` | — | — | Python logging level. When unset, derived from ENV: DEBUG in development, INFO everywhere else. |
| `TESTING` | `bool` | `False` | — | Test-only flag. Don't set in production. |
| `SECRET_KEY` | `SecretStr` | `SecretStr('')` | — | Signs JWTs + session cookies. Must be set in production (≥32 chars). |
| `BASE_URL` | `str` | `'http://127.0.0.1:8000'` | — | Public backend URL — used when building absolute links in emails, webhooks. |
| `FRONTEND_BASE_URL` | `str` | `'http://127.0.0.1:3000'` | — | Public frontend URL — used for post-auth redirects. |
| `CORS_ORIGINS` | `list[str]` | `[]` | — | Allowed CORS origins. Empty list disables cross-origin requests. |
| `USER_SESSION_TTL` | `datetime.timedelta` | `datetime.timedelta(days=31)` | — | How long a user session lasts before forcing re-auth. |
| `DATABASE_URL` | `str \| None` | — | — | Full DSN override. When set, wins over POSTGRES_* parts. Accepts the unprefixed DATABASE_URL env var too. |
| `POSTGRES_USER` | `str` | `'nagara'` | — | Postgres username. |
| `POSTGRES_PWD` | `SecretStr` | `SecretStr('**********')` | — | Postgres password. |
| `POSTGRES_HOST` | `str` | `'127.0.0.1'` | — | Postgres host. |
| `POSTGRES_PORT` | `int` | `5432` | ≥ 1, ≤ 65535 | Postgres port. Must be a valid TCP port. |
| `POSTGRES_DB` | `str` | `'nagara'` | — | Postgres database name. |
| `DATABASE_POOL_SIZE` | `int` | `5` | ≥ 1, ≤ 500 | SQLAlchemy connection pool size. Tune per replica. |
| `DATABASE_POOL_RECYCLE_SECONDS` | `int` | `600` | ≥ 1 | Seconds before a pooled connection is recycled. Guards against stale conns. |
| `DATABASE_COMMAND_TIMEOUT_SECONDS` | `float` | `30.0` | > 0 | Per-query timeout. 0 is not a valid value — prevents runaway queries. |
