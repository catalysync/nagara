apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nagara-core.fullname" . }}
  labels:
    {{- include "nagara-core.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
